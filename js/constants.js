/**
 * Grid and puzzle constants.
 */
const GRID_SIZE = 9;
const TOTAL_CELLS = 81;
const BOX_SIZE = 3;

/**
 * Arrow key to offset mapping.
 */
const ARROW_MOVES = {
  ArrowRight: 1,
  ArrowLeft: -1,
  ArrowDown: GRID_SIZE,
  ArrowUp: -GRID_SIZE,
};
