/**
 * render.js tests intentionally omitted.
 *
 * The render module is entirely DOM-coupled — it reads and writes DOM nodes,
 * queries elements by ID, and sets innerHTML. None of it can be tested outside
 * a real browser without mocks or fakes, which this project does not use.
 */
