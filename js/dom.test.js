/**
 * dom.js tests intentionally omitted.
 *
 * The dom module is entirely browser-coupled — event handlers, window.location,
 * prompt(), and history. None of it can be tested outside a real browser without
 * mocks or fakes, which this project does not use.
 */
