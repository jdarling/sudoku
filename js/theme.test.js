/**
 * theme.js tests intentionally omitted.
 *
 * The theme module reads and writes DOM attributes and interacts with
 * localStorage. None of it can be tested outside a real browser without
 * mocks or fakes, which this project does not use.
 */
